language: node_js
node_js:
  - "0.12"
before_install:
  - npm install -g grunt-cli
  - npm install -g casperjs@1.1.0-beta3
before_script:  
  - phantomjs --version
  - casperjs --version
after_success:
  - bash deploy.sh

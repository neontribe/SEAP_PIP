language: node_js
node_js:
  - "0.12"
before_install:
  - npm install -g grunt-cli
  - npm install -g casperjs
before_script:  
  - phantomjs --version
  - casperjs --version
after_success:
  - bash deploy.sh
